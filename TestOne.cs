using System;
using UnityEngine;

namespace TestGitHub
{
    public class TestOneClass
    {
        private int _id;
        private string _name;
        private string _health;

        public TestOneClass(int id, string name,string health)
        {
            _id = id;
            _name = name;
            _health = health;
        }
    }
}